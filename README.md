# Marlo — Coming soon

Minimal one-page landing for Marlo.

## Local

Open `index.html` in a browser, or:

```bash
python3 -m http.server 8080
```

Then visit http://127.0.0.1:8080

## GitHub Pages

Settings → Pages → Deploy from branch `main` / root (`/`).
